<%- include("partials/header.ejs") %>

<h1>Welcome to the blog</h1>
<a href="/post">Add new post</a>
<a href="/edit">Edit posts</a>

<body>
    <% if (locals.posts.length > 0) { %>        
        <ul>
            <% console.log(posts) %>
            <% for(let i=0; i<locals.posts.length; i++) { %> 
                <li>            
                    <form action="/delete" method="POST">
                        <h4 name="id"><%= posts[i].id %></h4>
                        <h2 name="title"><%= posts[i].title %></h2>
                        <h3 name="subheading"><%= posts[i].subheading %></h3>
                        <p name="content"><%= posts[i].content %></p>
                        <button name="delete" value="delete">Delete</button>

                    </form>
                </li>
            <% } %>
        </ul>
    <% } else { %>
        <% console.log(posts.length) %>
        <h2>No posts yet</h2>
    <% } %>


</body>
